data = ames_train_num,
label = train_labels
)
ames_test_matrix <- xgb.DMatrix(
data = ames_test_num,
label = test_labels
)
# Definir rango de hiperparámetros para la optimización
eta_values <- c(0.01, 0.05, 0.1, 0.3)
nrounds_values <- c(500, 1000, 2000)
best_rmse <- Inf
best_params <- list()
cv_results_df <- data.frame()
set.seed(123)
for (eta in eta_values) {
for (nrounds in nrounds_values) {
cv_results <- xgb.cv(
data = ames_train_matrix,
params = list(
eta = eta,
max_depth = 6,
subsample = 0.8,
colsample_bytree = 0.8,
objective = "reg:squarederror"
),
nrounds = nrounds,
nfold = 5,
metrics = "rmse",
verbose = 0,
early_stopping_rounds = 10
)
if (is.null(cv_results)) next
results_row <- data.frame(
eta = eta,
nrounds = nrounds,
min_rmse = min(cv_results$evaluation_log$test_rmse_mean),
best_nrounds = cv_results$evaluation_log$iter[which.min(cv_results$evaluation_log$test_rmse_mean)]
)
cv_results_df <- bind_rows(cv_results_df, results_row)
if (results_row$min_rmse < best_rmse) {
best_rmse <- results_row$min_rmse
best_params <- list(
eta = results_row$eta,
nrounds = results_row$best_nrounds
)
}
cat("\n, Best hyperparameters values found:\n")
cat("Eta:", best_params$eta, "\n")
cat("Nrounds:", best_params$nrounds, "\n")
cat("RMSE mínimo:", round(best_rmse, 4), "\n")
# View(cv_results_df)
getwd()
if(!require(animation)) install.packages('animation')
library(animation)
ani.options(interval = 0.5, nmax = 10)
xx = grad.desc()
ani.options(interval = 1.5, nmax = 10)
xx = grad.desc()
ani.options(interval = 2.5, nmax = 10)
xx = grad.desc()
library(plot3D)
# Define the squared error loss function
squared_error <- function(y, y_hat) {
return(0.5 * (y - y_hat)^2)
}
# Define the logistic activation function
logistic <- function(z) {
return(1 / (1 + exp(-z)))
}
# Generate data
x <- seq(-10, 10, length.out = 200)
y <- seq(-10, 10, length.out = 200)
z <- outer(x, y, FUN = function(x, y) squared_error(1, logistic(x + y)))
# Plot the exaggerated loss surface
persp3D(x, y, z, theta = 30, phi = 30, expand = 0.5, col = "lightblue", ticktype = "detailed", xlab = "Weight 1", ylab = "Weight 2", zlab = "Loss")
# Cargar librerías
library(ggplot2)
# Definir el rango de valores para z
z <- seq(-6, 6, length.out = 100)
# Función sigmoide
sigma <- 1 / (1 + exp(-z))
# Función de pérdida MSE para y=1 y y=0
mse_y1 <- (1 - sigma)^2
mse_y0 <- sigma^2
# Función de pérdida de entropía cruzada para y=1 y y=0
cross_entropy_y1 <- -log(sigma)
cross_entropy_y0 <- -log(1 - sigma)
# Crear dataframes para graficar
data_mse <- data.frame(z, mse_y1, mse_y0)
data_ce <- data.frame(z, cross_entropy_y1, cross_entropy_y0)
# Graficar MSE
p1 <- ggplot(data_mse, aes(x = z)) +
geom_line(aes(y = mse_y1, color = "MSE(y=1)")) +
geom_line(aes(y = mse_y0, color = "MSE(y=0)")) +
labs(title = "Mean Squared Error (MSE)", x = "z = w^T x", y = "Loss") +
theme_minimal() +
scale_color_manual(values = c("blue", "red")) +
theme(legend.title = element_blank())
# Graficar Cross-Entropy
p2 <- ggplot(data_ce, aes(x = z)) +
geom_line(aes(y = cross_entropy_y1, color = "CE(y=1)")) +
geom_line(aes(y = cross_entropy_y0, color = "CE(y=0)")) +
labs(title = "Cross-Entropy Loss", x = "z = w^T x", y = "Loss") +
theme_minimal() +
scale_color_manual(values = c("blue", "red")) +
theme(legend.title = element_blank())
# Mostrar ambas gráficas juntas
library(gridExtra)
grid.arrange(p1, p2, ncol = 2)
# Cargar librerías necesarias
library(ggplot2)
# Definir el rango de valores para z
z <- seq(-6, 6, length.out = 100)
# Función sigmoide
sigma <- 1 / (1 + exp(-z))
# Función de pérdida MSE para y=1 y y=0
mse_y1 <- (1 - sigma)^2
mse_y0 <- sigma^2
# Función de pérdida de entropía cruzada para y=1 y y=0
cross_entropy_y1 <- -log(sigma)
cross_entropy_y0 <- -log(1 - sigma)
# Crear data frames para facilitar la visualización
df_mse <- data.frame(z = rep(z, 2),
loss = c(mse_y1, mse_y0),
y = factor(rep(c("y = 1", "y = 0"), each = length(z))))
df_cross_entropy <- data.frame(z = rep(z, 2),
loss = c(cross_entropy_y1, cross_entropy_y0),
y = factor(rep(c("y = 1", "y = 0"), each = length(z))))
# Graficar MSE
p1 <- ggplot(df_mse, aes(x = z, y = loss, color = y)) +
geom_line() +
labs(title = "Mean Squared Error (MSE)", x = "z = w^T x", y = "Loss") +
theme_minimal() +
scale_color_manual(values = c("blue", "red"))
# Graficar Cross-Entropy
p2 <- ggplot(df_cross_entropy, aes(x = z, y = loss, color = y)) +
geom_line() +
labs(title = "Cross-Entropy Loss", x = "z = w^T x", y = "Loss") +
theme_minimal() +
scale_color_manual(values = c("blue", "red"))
# Mostrar ambas gráficas
library(gridExtra)
grid.arrange(p1, p2, ncol = 2)
# Cargar librerías
library(ggplot2)
library(plotly)
# Definir el rango de valores para z
z <- seq(-6, 6, length.out = 100)
# Función sigmoide
sigma <- 1 / (1 + exp(-z))
# Función de pérdida MSE para y=1 y y=0
mse_y1 <- (1 - sigma)^2
mse_y0 <- sigma^2
# Función de pérdida de entropía cruzada para y=1 y y=0
cross_entropy_y1 <- -log(sigma)
cross_entropy_y0 <- -log(1 - sigma)
# Crear dataframes para graficar
data_mse <- data.frame(z, mse_y1, mse_y0)
data_ce <- data.frame(z, cross_entropy_y1, cross_entropy_y0)
# Graficar MSE en función de z
p1 <- ggplot(data_mse, aes(x = z)) +
geom_line(aes(y = mse_y1, color = "MSE(y=1)")) +
geom_line(aes(y = mse_y0, color = "MSE(y=0)")) +
labs(title = "MSE Loss vs z", x = "z = w^T x", y = "Loss") +
theme_minimal() +
scale_color_manual(values = c("blue", "red")) +
theme(legend.title = element_blank())
# Graficar Cross-Entropy en función de z
p2 <- ggplot(data_ce, aes(x = z)) +
geom_line(aes(y = cross_entropy_y1, color = "CE(y=1)")) +
geom_line(aes(y = cross_entropy_y0, color = "CE(y=0)")) +
labs(title = "Cross-Entropy Loss vs z", x = "z = w^T x", y = "Loss") +
theme_minimal() +
scale_color_manual(values = c("blue", "red")) +
theme(legend.title = element_blank())
# Definir un grid de valores de w1 y w2
w1 <- seq(-3, 3, length.out = 50)
w2 <- seq(-3, 3, length.out = 50)
grid <- expand.grid(w1 = w1, w2 = w2)
# Definir una función sigmoide y calcular la pérdida para MSE
sigmoid <- function(z) { 1 / (1 + exp(-z)) }
grid$z <- grid$w1 * 0.5 + grid$w2 * (-0.5)  # Simular una combinación lineal
grid$mse_loss <- (1 - sigmoid(grid$z))^2 + sigmoid(grid$z)^2  # MSE loss
# Definir la pérdida para Cross-Entropy
grid$ce_loss <- -log(sigmoid(grid$z)) - log(1 - sigmoid(grid$z))  # CE loss
# Graficar MSE en el espacio de pesos
p3 <- ggplot(grid, aes(x = w1, y = w2, fill = mse_loss)) +
geom_tile() +
labs(title = "MSE Loss Surface", x = "w1", y = "w2", fill = "Loss") +
theme_minimal() +
scale_fill_viridis_c()
# Graficar Cross-Entropy en el espacio de pesos
p4 <- ggplot(grid, aes(x = w1, y = w2, fill = ce_loss)) +
geom_tile() +
labs(title = "Cross-Entropy Loss Surface", x = "w1", y = "w2", fill = "Loss") +
theme_minimal() +
scale_fill_viridis_c()
# Mostrar todas las gráficas juntas
library(gridExtra)
grid.arrange(p1, p3, p2, p4, ncol = 2)
# Cargar librerías necesarias
library(ggplot2)
library(plotly)
# Definir un rango de valores para w1 y w2
w1 <- seq(-3, 3, length.out = 50)
w2 <- seq(-3, 3, length.out = 50)
grid <- expand.grid(w1 = w1, w2 = w2)
# Definir una función sigmoide
sigmoid <- function(z) { 1 / (1 + exp(-z)) }
# Simular una combinación lineal de los pesos
grid$z <- grid$w1 * 0.5 + grid$w2 * (-0.5)
# Función de pérdida MSE
grid$mse_loss <- (1 - sigmoid(grid$z))^2 + sigmoid(grid$z)^2
# Función de pérdida Cross-Entropy
grid$ce_loss <- -log(sigmoid(grid$z)) - log(1 - sigmoid(grid$z))
# Crear gráfico 3D para MSE
p_mse <- plot_ly(grid, x = ~w1, y = ~w2, z = ~mse_loss, type = "surface") %>%
layout(title = "MSE Loss Surface",
scene = list(xaxis = list(title = "w1"),
yaxis = list(title = "w2"),
zaxis = list(title = "Loss")))
# Crear gráfico 3D para Cross-Entropy
p_ce <- plot_ly(grid, x = ~w1, y = ~w2, z = ~ce_loss, type = "surface") %>%
layout(title = "Cross-Entropy Loss Surface",
scene = list(xaxis = list(title = "w1"),
yaxis = list(title = "w2"),
zaxis = list(title = "Loss")))
# Mostrar gráficos interactivos
p_mse
p_ce
? ani.options
? animation::ani.options
hidden_layers <- c(4,2)  # Two hidden layers with 4 and 2 neurons
activation_function <- "logistic"  # Sigmoid
optimizer <- "rprop+"  # Optimizer
learning_rate <- 0.01  # Learning rate
threshold_value <- 0.01  # Convergence  thresholod
max_iterations <- 1e6  # Max iterations
hidden_layers <- c(4,2)  # Two hidden layers with 4 and 2 neurons
activation_function <- "logistic"  # Sigmoid
optimizer <- "rprop+"  # Optimizer
learning_rate <- 0.01  # Learning rate
threshold_value <- 0.01  # Convergence  thresholod
max_iterations <- 1e6  # Max iterations
nn <- neuralnet(dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
#Neural Network
library(neuralnet)
nn <- neuralnet(dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=c(2,1),
linear.output=FALSE,
threshold=0.01)
if (!require(neuralnet))
install.packages("neuralnet", dep=TRUE)
mydata <- read.csv("https://raw.githubusercontent.com/MGCodesandStats/datasets/master/dividendinfo.csv")
str(mydata)
normalize <- function(x) {
return ((x - min(x)) / (max(x) - min(x)))
}
normData <- as.data.frame(lapply(mydata, normalize))
perc2Train <- 2/3
ssize <- nrow(normData)
set.seed(12345)
data_rows <- floor(perc2Train *ssize)
train_indices <- sample(c(1:ssize), data_rows)
trainset <- normData[train_indices,]
testset <- normData[-train_indices,]
hidden_layers <- c(4,2)  # Two hidden layers with 4 and 2 neurons
activation_function <- "logistic"  # Sigmoid
optimizer <- "rprop+"  # Optimizer
learning_rate <- 0.01  # Learning rate
threshold_value <- 0.01  # Convergence  thresholod
max_iterations <- 1e6  # Max iterations
#Neural Network
library(neuralnet)
nn <- neuralnet(dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=c(2,1),
linear.output=FALSE,
threshold=0.01)
nn2 <- neuralnet(dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
plot(nn, rep = "best")
plot(nn2, rep = "best")
round_weights <- function(nn_model, decimals=2) {
nn_model$weights <- lapply(nn_model$weights, function(layer) round(layer, decimals))
return(nn_model)
}
nn_rounded <- round_weights(nn2, 2)
round_weights(nn2, 2)
round_weights(nn, 2)
print(nn, digits=2)  # Esto redondea los pesos a dos decimales
plot(nn2, rep = "best")
round_weights <- function(nn_model, decimals=2) {
nn_model$weights <- lapply(nn_model$weights, function(layer) {
lapply(layer, function(weight_matrix) {
if (is.numeric(weight_matrix)) {
round(weight_matrix, decimals)
} else {
weight_matrix
}
})
return(nn_model)
}
# Aplicamos la función al modelo entrenado
nn_rounded <- round_weights(nn, 2)
# Ahora visualizamos la red con los valores redondeados
plot(nn_rounded, rep = "best")
plot(nn, rep = "best")
nn <- neuralnet(dividend ~ .,
# fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
# Neural Network
library(neuralnet)
nn <- neuralnet(dividend ~ .,
# fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
plot(nn, rep = "best")
round_weights <- function(nn_model, decimals=2) {
nn_model$weights <- lapply(nn_model$weights, function(layer) {
lapply(layer, function(weight_matrix) {
if (is.numeric(weight_matrix)) {
round(weight_matrix, decimals)
} else {
weight_matrix
}
})
return(nn_model)
}
# Aplicamos la función al modelo entrenado
nn_rounded <- round_weights(nn, 2)
# Ahora visualizamos la red con los valores redondeados
plot(nn_rounded, rep = "best")
nn <- neuralnet(#dividend ~ .,
dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
plot(nn, rep = "best")
# Neural Network
library(neuralnet)
nn <- neuralnet(#dividend ~ .,
dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
plot(nn, rep = "best")
roundedresults<-sapply(results,round,digits=0)
roundedresultsdf=data.frame(roundedresults)
roundedresults<-sapply(results,round,digits=0)
if (!require(neuralnet))
install.packages("neuralnet", dep=TRUE)
mydata <- read.csv("https://raw.githubusercontent.com/MGCodesandStats/datasets/master/dividendinfo.csv")
str(mydata)
normalize <- function(x) {
return ((x - min(x)) / (max(x) - min(x)))
}
normData <- as.data.frame(lapply(mydata, normalize))
perc2Train <- 2/3
ssize <- nrow(normData)
set.seed(12345)
data_rows <- floor(perc2Train *ssize)
train_indices <- sample(c(1:ssize), data_rows)
trainset <- normData[train_indices,]
testset <- normData[-train_indices,]
hidden_layers <- c(4,2)  # Two hidden layers with 4 and 2 neurons
activation_function <- "logistic"  # Sigmoid
optimizer <- "rprop+"  # Optimizer
learning_rate <- 0.01  # Learning rate
threshold_value <- 0.01  # Convergence  thresholod
max_iterations <- 1e6  # Max iterations
# Neural Network
library(neuralnet)
nn <- neuralnet(#dividend ~ .,
dividend ~ fcfps + earnings_growth + de + mcap + current_ratio,
data=trainset,
hidden=hidden_layers,
act.fct=activation_function,
algorithm=optimizer,
learningrate=learning_rate,
linear.output=FALSE,
threshold=threshold_value,
stepmax=max_iterations)
plot(nn, rep = "best")
round_weights <- function(nn_model, decimals=2) {
nn_model$weights <- lapply(nn_model$weights, function(layer) {
lapply(layer, function(weight_matrix) {
if (is.numeric(weight_matrix)) {
round(weight_matrix, decimals)
} else {
weight_matrix
}
})
return(nn_model)
}
# Apply function to trained net
nn_rounded <- round_weights(nn, 2)
# View network with less decimals
plot(nn_rounded, rep = "best")
temp_test <- subset(testset, select =
c("fcfps","earnings_growth",
"de", "mcap", "current_ratio"))
nn.results <- compute(nn, temp_test)
results <- data.frame(actual =
testset$dividend,
prediction = nn.results$net.result)
head(results)
roundedresults<-sapply(results,round,digits=0)
roundedresultsdf=data.frame(roundedresults)
attach(roundedresultsdf)
table(actual,prediction)
# Round predictions to obtain binary values (0 or 1)
roundedresults <- sapply(results, round, digits=0)
roundedresultsdf <- data.frame(roundedresults)
# Convert variables to factors for proper processing in `caret`
roundedresultsdf$actual <- as.factor(roundedresultsdf$actual)
roundedresultsdf$prediction <- as.factor(roundedresultsdf$prediction)
# Load caret package (install if not available)
if (!require(caret)) install.packages("caret", dependencies=TRUE)
library(caret)
# Generate confusion matrix with advanced metrics
conf_matrix <- confusionMatrix(roundedresultsdf$prediction, roundedresultsdf$actual)
# Print the confusion matrix and performance metrics
print(conf_matrix)
preproc <- preProcess(mydata, method = c("range"))  # Min-Max normalization
normData <- predict(preproc, mydata)
# Print model summary
print(nn_model)
quarto preview
quarto::render()
quarto::quarto_render()
quarto::quarto_preview()
# Chunk 1: packages
if (!require(mlbench)) install.packages("mlbench", dep=TRUE)
# Chunk 2
knitr::include_graphics("images/decTree4HypertensionNew.png")
# Chunk 3
knitr::include_graphics("images/1.1-DecisionTrees-Slides_insertimage_1.png")
knitr::include_graphics("images/1.1-DecisionTrees-Slides_insertimage_2.png")
# Chunk 4
knitr::include_graphics("images/clipboard-58173806.png")
# Chunk 5
knitr::include_graphics("images/splits_nodes_1.png")
# Chunk 6
knitr::include_graphics("images/splits_nodes_2.png")
# Chunk 7
knitr::include_graphics("images/splits_nodes_3.png")
# Chunk 8
knitr::include_graphics("images/1.1-DecisionTrees-Slides_insertimage_3.png")
# Chunk 9
knitr::include_graphics("images/BadSplit.png")
# Chunk 10
knitr::include_graphics("images/GoodSplit.png")
# Chunk 11
knitr::include_graphics("images/impurity.jpg")
# Chunk 12
knitr::include_graphics("images/Example2-EntropyCalculation.png")
# Chunk 13
knitr::include_graphics("images/Example2-IGCalculation.png")
# Chunk 14
data("PimaIndiansDiabetes2", package = "mlbench")
dplyr::glimpse(PimaIndiansDiabetes2)
library(tidyverse)
library(mlbench)
data(PimaIndiansDiabetes2)
vars <- c("glucose", "insulin", "mass", "pressure", "age")
PimaIndiansDiabetes2 %>%
select(all_of(vars), diabetes) %>%
pivot_longer(-diabetes, names_to = "variable", values_to = "value") %>%
ggplot(aes(x = diabetes, y = value)) +
geom_boxplot(outlier.alpha = 0.3) +
facet_wrap(~variable, scales = "free_y") +
theme_minimal()
